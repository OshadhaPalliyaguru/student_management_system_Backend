spring:
  datasource:
    url: jdbc:mysql://localhost:3307/student_system?createDatabaseIfNotExist=true
    username:
    password:

    jpa:
      hibernate:
        ddl-auto: update
      show_sql: true